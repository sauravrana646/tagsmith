# Phase 2 golden set
#
# Grow this toward 100–200 labeled emails before trusting RAG (Phase 3).
# Regenerated by: `uv run python evals/generate_golden_set.py`
# Current seed is synthetic-but-realistic coverage of the taxonomy (+ holds).
# Live LLM baseline / threshold tuning: deferred until provider keys are available.
#
# Format: one JSON object per line (GoldenCase). See docs/EVALS.md.
#
# Offline CI: `uv run python evals/run_eval.py --rules-only`
# Live LLM:   `uv run python evals/run_eval.py` (requires provider API keys)
