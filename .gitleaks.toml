# Gitleaks configuration for Tagsmith
title = "tagsmith"

[allowlist]
description = "Allow benign example placeholders (not the whole docs tree)"
paths = [
  '''^\.env\.example$''',
  '''^README\.md$''',
]
